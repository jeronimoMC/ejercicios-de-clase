let ocupacionCancha = true;
let nombreCancha = "Bernabeu";

let mensaje = ocupacionCancha ? `la cancha ${nombreCancha}esta ocupada"` : 
`la cancha ${nombreCancha} esta disponible"` ;
console.log (mensaje);